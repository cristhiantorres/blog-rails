<% @articles.each do |article| %>
	<!-- <h2><%= article.title %></h2>
	<div>
		<%= article.body %> - <%= link_to "Eliminar", article , method: :delete, class: 'btn btn-default' %>
	</div> -->

	<div class="panel panel-default">
  		<div class="panel-heading"><%= link_to article.title, article %> - Visitas: <%= article.visits_count %></div>
  		<div class="panel-body">
  			<%= article.body %>
  		</div>
  		<div class="panel-footer">
  			<%= link_to "Eliminar", article , method: :delete, class: 'btn btn-danger' %> 
  			<%= link_to "Editar", edit_article_path(article.id), class: 'btn btn-primary' %>
  		</div>
</div>
<%end%>